# 待优化清单

> 审查日期：2026-09-14
> 审查范围：固件（`main/`）、后端（`backend/`）、工程化（脚本 / 配置 / 版本控制）
> 条目格式：**位置** → 问题 → 建议 → 优先级（高 / 中 / 低）
> 统计：高优先级 26 项，中优先级 40 项，低优先级 21 项，共 87 项
> 进度：一、固件（项 1-35）已全部完成（2026-09-14）；二、后端（项 36-71）与三、工程化（项 72-87）已于 2026-09-14 完成主要修复，`pytest` 9 项通过

## 目录

- [一、固件（main/）](#一固件main)
  - [1.1 内存](#11-内存) · [1.2 并发与竞态](#12-并发与竞态) · [1.3 性能](#13-性能) · [1.4 健壮性](#14-健壮性) · [1.5 代码质量](#15-代码质量) · [1.6 功耗](#16-功耗)
- [二、后端（backend/）](#二后端backend)
  - [2.1 安全](#21-安全) · [2.2 并发与异步](#22-并发与异步) · [2.3 性能](#23-性能) · [2.4 健壮性](#24-健壮性) · [2.5 代码质量](#25-代码质量) · [2.6 依赖与部署](#26-依赖与部署)
- [三、工程化](#三工程化)
  - [3.1 版本控制](#31-版本控制) · [3.2 构建与脚本](#32-构建与脚本) · [3.3 配置管理](#33-配置管理) · [3.4 测试与 CI](#34-测试与-ci)
- [四、建议实施顺序](#四建议实施顺序)

---

## 一、固件（main/）

### 1.1 内存

**1. [高] 表情双缓冲约 32KB 落在内部 DRAM**
- 位置：`main/ui.c:64-65`（`frame_buffer_a/b`，各 90×90×2 ≈ 16KB）
- 问题：`.bss` 默认进内部 DRAM，而 `sdkconfig` 未开 `SPIRAM_ALLOW_BSS_SEG_EXTERNAL`；N16R8 内部 RAM 需供养 WiFi/BT/LVGL，余量紧张。
- 建议：开 `CONFIG_SPIRAM_ALLOW_BSS_SEG_EXTERNAL_MEMORY`，或改 `psram_malloc` / `EXT_RAM_BSS_ATTR`。
- 收益：释放约 32KB 内部连续块，降低 BLE/WiFi 共存 OOM 风险。
- 状态：**已修复**（2026-09-14）：`ui.c` 帧缓冲改 `psram_malloc` 运行时分配（含失败降级与空指针保护）；map 文件确认静态 BSS 从 32KB 降至 12 字节（3 个指针）。

**2. [高] UI 消息队列体积过大**
- 位置：`main/ui_msg.h:22`（`char json[1024]`）+ `main/ui.c:1596`（`xQueueCreate(16, sizeof(ui_msg_t))`）
- 问题：单条消息约 1.1KB，队列合计约 17KB 内部 DRAM；多数消息只用几个字段，且每次入队全量拷贝。
- 建议：队列只传小句柄/指针，大 JSON 放 PSRAM；或按消息类型用 union 缩小结构体。
- 收益：省十余 KB 内部 RAM，减少拷贝开销。
- 状态：**已修复**（2026-09-14）：`json` 字段改为 PSRAM 指针传递（`ui_post_event_json` 分配、`process_messages` 消费后 `free`、入队失败即释放）；`ui_msg_t` 从约 1.1KB 降至约 48B，队列占用 17KB → 约 0.8KB，LVGL 任务栈拷贝同步减小。

**3. [高] WS 音频槽整包拷贝到栈，有溢出风险**
- 位置：`main/voice.c:289`（`drain_ws_audio_queue` 内 `ws_audio_slot_t slot;`）
- 问题：槽含 `data[4096]`，栈拷贝 4KB+，而 `app` 任务栈仅 8192 字节；嵌套 `audio_playback_write` / 网络发送时极易溢出。
- 建议：持锁只取指针/索引，处理完再推进 tail；或改用静态/PSRAM 暂存槽。
- 收益：消除高概率栈溢出重启。
- 状态：**已修复**（2026-09-14）：`voice.c` 新增 PSRAM 暂存槽 `s_ws_audio_drain`（`voice_init` 分配，PSRAM 优先、内部 RAM 兜底），drain 不再做 4KB 栈拷贝。

**4. [中] `audio_init` 失败路径资源泄漏**
- 位置：`main/audio.c:154-163`
- 问题：分配中途失败直接返回 `ESP_ERR_NO_MEM`，已成功的 `psram_malloc` / mutex 未释放。
- 建议：失败路径统一 `goto cleanup`。
- 状态：**已修复**（2026-09-14）：`audio_init` 失败路径释放已分配缓冲并删除 mutex，指针归 NULL。

**5. [中] 动画帧表分配与 mmap 泄漏**
- 位置：`main/anim_loader.c:59`、`:109-111`
- 问题：帧表 `malloc(total_frames * sizeof(frame_data_t))` 走默认堆（常进内部 RAM）；`parse_blob` 失败时未 `esp_partition_munmap`。
- 建议：帧表改 `psram_malloc`；失败路径补 munmap + free。
- 状态：**已修复**（2026-09-14）：帧表改 `psram_malloc`（内部 RAM 兜底）；`parse_blob` 失败时 `esp_partition_munmap` 并清空 `s_blob`。

**6. [中] 录/播大缓冲并存，PSRAM 峰值与碎片**
- 位置：`main/voice.c:357-360`（录音 640KB + 播放环 512KB + 16×4KB 音频队列 + CJK 字体约 900KB 拷贝）
- 建议：录音改环形/流式（已有流式上传可缩短 `VOICE_MAX_RECORD_BYTES`）；运行期监控 `heap_caps_get_largest_free_block`。
- 状态：**部分完成**（2026-09-14）：`audio_init` 末尾新增 `mem_report("audio")`（覆盖录/播大缓冲分配后的峰值观测，补 `boot` 报告盲区）；录音流式化属架构调整，留待后续评估。

**7. [低] 字库加载二次拷贝**
- 位置：`main/font_loader.c:59-64`
- 问题：`lv_binfont_create_from_buffer` 将字形再拷一份到堆，峰值占用高。
- 建议：评估直接 mmap 只读字体或进一步压缩子集。
- 状态：**已评估，保持现状**（2026-09-14）：LVGL 9.2 `lv_binfont_create_from_buffer` 在创建时即将全量字形读入 `lv_malloc` RAM（→PSRAM），flash mmap 本身不占 RAM，不存在二次 RAM 峰值，当前实现正确；后续唯一杠杆是生成端（`gen_cjk_font.py`）进一步裁剪字样子集。

### 1.2 并发与竞态

**8. [高] 动画帧索引跨任务无锁读写**
- 位置：`main/main.c:52-59`（`app` 任务 `ui_tick_animation` 写）vs `main/ui.c:1642-1659`（`lvgl` 任务渲染读）
- 问题：两任务同在 Core 1 但优先级不同（3 vs 4），可相互抢占，`frame_index` / `frame_elapsed_ms` 无同步，可能花屏/跳帧。
- 建议：动画推进只在 LVGL 任务内做；或用原子变量/队列投递「下一帧索引」。
- 状态：**已修复**（2026-09-14）：`ui_tick_animation` 从 `app` 任务移入 `ui_loop_once`（lvgl 任务），帧推进后直接调 `render_frame()`，不再经队列中转；`frame_index`/`frame_elapsed_ms` 现为 lvgl 单任务读写。

**9. [高] 录音缓冲读写无锁，上传撕裂**
- 位置：`main/audio.c:406-408`、`:481-534`
- 问题：`voice_net_poll` 边读 PCM 边上传，同时 `audio_task` 在写 `s_record`，长度/数据无快照，可能发出半包；`s_fade_out_*`、`s_volume_percent` 同样无锁。
- 建议：锁内快照长度+指针；音量/淡出用原子或同一 mutex。
- 状态：**已修复**（2026-09-14）：新增 `audio_capture_snapshot()`（锁内取指针+长度），`voice.c` 上传/丢弃路径全部改用快照；`audio_playback_begin_fadeout`/`cancel_fadeout` 加锁保证多字段一致。录音仅追加在 size 之后，快照后读 [0, size) 无重叠。

**10. [高] DMA 未完成即 `flush_ready`**
- 位置：`main/display.c:33`
- 问题：`esp_lcd_panel_draw_bitmap` 返回后立刻 `lv_display_flush_ready`，SPI DMA 可能仍在传输，LVGL 复用缓冲导致局部花屏。
- 建议：在 IO 完成回调（`on_color_trans_done`）中调 `flush_ready`，或同步等待传输完成。
- 状态：**已修复**（2026-09-14）：注册 `on_color_trans_done` 回调（IDF 保证一次 draw_bitmap 仅在末段事务后回调一次），`flush_cb` 与 `display_blit_rgb565` 共用二值信号量等待 DMA 完成（100ms 超时兜底）；回调在面板初始化完成后注册，避免残留信号。

**11. [高] WebSocket 事件回调内做重活**
- 位置：`main/net_ws.c` `handle_text`（约 :348-420）
- 问题：在 WS/TCPIP 任务上下文里 `cJSON_Parse`、`ui_post_event_json`（可阻塞 20ms）、回发 pong、改语音状态，易引发断连与优先级反转。
- 建议：回调只入队，由 `net`/`app` 任务消化。
- 状态：**已修复**（2026-09-14）：新增文本帧队列（8 槽 × ≤2KB，PSRAM 分配），WS 回调只拷贝入队，`net_loop`（net 任务）统一消化 `handle_text`；超大/满队列帧丢弃并告警。二进制音频帧维持原有锁保护环形队列不变。

**12. [高] `app` 任务内长阻塞等 WS 事件**
- 位置：`main/voice.c` + `main/net_ws.c:900/933`（`xEventGroupWaitBits(..., 2500ms)`）
- 问题：发送 audio_stream_begin/upload 时 `app` 任务最长阻塞 2.5s，同任务还跑 VAD 与 `ui_tick`，UI/语音被卡死。
- 建议：改异步状态机（发 begin → 等 session 事件回调），禁止 `app` 长阻塞。
- 状态：**已修复**（2026-09-14）：新异步握手 API `net_ws_audio_session_start`/`poll` 取代阻塞式 `net_ws_send_audio_stream_begin`/`upload`；`voice_net_poll` 轮询就绪、2.5s 超时回退整包上传，握手期间录音不中断、app 任务零阻塞。注：回退路径的 PCM 整包 `ws_send_bin` 传输本身仍占 app 任务（仅握手失败时触发，罕见）。

**13. [中] `xQueueSend` 不检查返回值**
- 位置：`main/ui.c:1520`（`xQueueSend(..., 20ms)`）
- 问题：队列满时静默丢事件（状态/字幕丢失）。
- 建议：合并同类消息；失败打日志或覆盖保留最新。
- 状态：**已修复**（2026-09-14）：`ui_post` 返回发送结果，失败限频告警（每 50 次一条，含类型与累计数）；事件 JSON 入队失败即释放并告警（1.1 节已含）；FRAME_DIRTY/mic level 本就有合并逻辑。

**14. [中] GATT 回调路径无限等锁**
- 位置：`main/ble_prov.c:281`（`xSemaphoreTake(..., portMAX_DELAY)`）
- 问题：NimBLE host 上下文内无限等锁有死锁风险。
- 建议：改短超时；写路径只入环形缓冲，业务在 `ble_prov_loop` 处理。
- 状态：**已修复**（2026-09-14）：GATT 写回调等锁改 10ms 超时，超时返回 `BLE_ATT_ERR_UNLIKELY` 丢弃该写，NimBLE host 不再可能无限阻塞。

### 1.3 性能

**15. [高] 表情帧未变化也每 5ms 全量 SPI 刷新**
- 位置：`main/ui.c:1629-1639`（`blit_face_frame()`）
- 问题：LVGL 循环约 5ms 一次，静态帧也全量推 90×90×2 ≈ 16KB（约 3MB/s 总线空转），还与 LVGL partial flush 抢同一 SPI。
- 建议：仅帧脏（`FRAME_DIRTY`）/内容变化时 blit；静态帧跳过。
- 收益：SPI/CPU 占用大幅下降，UI 更跟手。
- 状态：**已修复**（2026-09-14）：新增 `s_face_dirty` 标记，`blit_face_frame()` 仅在脏时发起 SPI 传输；解码/蓝牙/AP 配网/诊断关闭等改动表情内容的路径统一置脏，静态帧零传输。

**16. [高] 脏帧双倍 SPI 传输**
- 位置：`main/ui.c`（`render_frame` 内已 blit，`ui_loop_once` 再 blit）
- 建议：解码只写后缓冲，统一在一处 blit，带宽减半。
- 状态：**已修复**（2026-09-14）：`render_frame()` 解码交换后只置脏不再直接 blit，全文件唯一 blit 点为 `ui_loop_once` 末尾，每轮最多一次 16KB 传输。

**17. [中] 播放环逐字节写入**
- 位置：`main/audio.c:526-529`
- 问题：热路径字节循环 + 多次加锁。
- 建议：按环形两段 `memcpy`，锁外准备数据。
- 状态：**已修复**（2026-09-14）：`audio_playback_write` 写入端与 `audio_playback_service` 读出端均改为环形两段 `memcpy`。

**18. [中] `audio_playback_write` 最长忙等 2s**
- 位置：`main/audio.c:507-510`
- 问题：从 `voice`/`app` 调用会长时间占住 Core 1。
- 建议：非阻塞写入 + 队列深度反压；满则丢或只写部分。
- 状态：**已修复**（2026-09-14）：总等待上限 2s → 200ms，超时丢弃剩余并 `ESP_LOGW` 告警，app 任务不再被播放环卡死。

**19. [中] 链路状态定时无差别上报**
- 位置：`main/main.c:70-73`（每 250ms `ui_post_link_state`）
- 问题：无变化也塞队列（每条约 1KB 拷贝）。
- 建议：状态变化才 post（mic 已有合并逻辑可照做）。
- 状态：**已修复**（2026-09-14）：缓存上次上报值，仅 usb/wifi/ws/听音/播放状态变化时 post；rssi 加 10dBm 滞回防抖。

**20. [低] LVGL 缓冲在 PSRAM 刷屏偏慢**
- 位置：`main/display.c:86-87`
- 说明：为保内部 RAM 给 BT 是合理权衡；若内部允许可改单小缓冲，或在屏幕稳定前提下提高 SPI 时钟。
- 状态：**已评估，保持现状**（2026-09-14）：双缓冲各 19.2KB（240×40×2），迁入内部需 38.4KB 连续 DRAM，会吃掉 1.1 节释放的空间并威胁 WiFi/BT 的大块分配；项 15/16 修复后 SPI 空转已消除，收益不足以覆盖风险。

### 1.4 健壮性

**21. [中] 离线无 STALE 超时切换**
- 位置：`main/board_pins.h` 已定义 `OFFLINE_TIMEOUT_MS` 但未使用
- 问题：离线判定仅跟 `ws_link`，半开连接可能长期不切 STALE/OFFLINE 动画。
- 建议：用最后事件时间 + `OFFLINE_TIMEOUT_MS` 触发 STALE。
- 状态：**已修复**（2026-09-14）：`net_ws` 新增 `net_ws_last_rx_ms()`（连接/任意帧刷新，含后端 2s 心跳 ping）；`refresh_face_display` 在 ws 在线但超 `OFFLINE_TIMEOUT_MS`(8s) 无帧时切 STALE 动画，voice_hold 期间不覆盖。

**22. [中] Task WDT 饿死只警告不复位**
- 位置：`sdkconfig`（`CONFIG_ESP_TASK_WDT_PANIC` 未开）
- 建议：生产开启 panic；关键任务 `esp_task_wdt_add`，死锁可自动恢复。
- 状态：**已修复**（2026-09-14）：`sdkconfig`+`sdkconfig.defaults` 开启 `CONFIG_ESP_TASK_WDT_PANIC=y`（5s）；app/lvgl/audio/net 四任务均 `esp_task_wdt_add(NULL)` + 循环内喂狗，任一卡死 5s 自动复位。

**23. [中] `show()` 手拼 JSON 无转义**
- 位置：`main/voice.c:102-117`
- 问题：`text` 含引号/反斜杠/换行会破坏 JSON，UI 解析失败、字幕偶发丢失。
- 建议：用 cJSON 生成或至少做 JSON 转义。
- 状态：**已修复**（2026-09-14）：`show()` 改用 cJSON 组包（自动转义引号/反斜杠/换行），同时解除旧 192B 固定缓冲的长度截断。

**24. [中] 分区表无 `ota_1`，无法安全 A/B OTA**
- 位置：`partitions.csv`（仅 `ota_0` + `otadata`）
- 建议：压缩 animations/font/storage 分区挤出第二 app 槽；或文档明确「单槽升级、失败需线刷」。
- 状态：**已修复**（2026-09-14）：分区表重排出 `app1/ota_1`(0x310000,3MB) A/B 槽；数据分区后移：cjk_font→0x610000、animations→0x810000(4.5MB)、storage→0xC80000(3.4MB)、coredump→0xFF0000，删除遗留空 spiffs。固件按 label 找分区无需改动；`flash_animations.py`/`flash_font.py`/`gen_cjk_font.py`/`font_size.h`/README/main.c 提示语已同步。**注意：下次烧录必须整表全刷并重刷字模/动画数据。**

**25. [中] WiFi 断线立即重试无退避**
- 位置：`main/net_ws.c`（断线事件直接 `esp_wifi_connect()`）
- 建议：指数退避（已有 `WIFI_RETRY_MS` 宏可复用），省电减 AP 压力。
- 状态：**已修复**（2026-09-14）：断线不再立即 `esp_wifi_connect()`，改 esp_timer 一次性退避重连（5s 起翻倍、封顶 60s）；拿到 IP 或 STA 重启时复位退避。

**26. [低] 动画/字体缺失无降级 UI**
- 位置：`main/main.c`（分区缺失仅 LOGE 继续跑）
- 建议：占位脸 + 错误态固定屏，便于产测诊断。
- 状态：**已修复**（2026-09-14）：动画缺失时渲染占位脸（红叉眼+平嘴，借 1.3 脏标记只刷一次）+ 状态栏「无动画资源」；字模缺失回退 montserrat_14 并显示「FONT MISSING」（ASCII 可显）。

### 1.5 代码质量

**27. [中] `ui.c` 1722 行单体**
- 建议：拆 `ui_face.c` / `ui_caption.c` / `ui_status_bar.c` / `ui_queue.c`，降低耦合与回归面。
- 状态：**部分完成**（2026-09-14）：抽出 `ui_face.c`（表情帧缓冲/RLE 解码/动画 tick/配网页脸/占位脸/按需 blit，约 240 行），`ui.c` 降至 1590 行，几何宏经 `ui_face.h` 共享；顺带清掉 1.2 后闲置的 `UI_MSG_FRAME_DIRTY` 队列通道与 `ui_post_frame_dirty` 死代码。caption/status_bar/queue 与 LVGL 控件状态耦合深，拆分收益低风险高，留待后续。

**28. [中] VAD/AGC/超时等魔法数字硬编码**
- 位置：`main/voice.c`、`main/audio.c`（`VAD_*`、`PDM_AGC_*`、`PLAY_STALL_MS` 等）
- 建议：迁入 `Kconfig.projbuild` 或 NVS，现场可调噪底。
- 状态：**已修复**（2026-09-14）：21 个参数迁入 `Kconfig.projbuild`「Voice / audio tuning」子菜单（VAD 阈值/噪声估计/超时 15 项 + PDM AGC/读超时 6 项），浮点按 x10/x100/x1000 定点化，代码宏直接映射 `CONFIG_AGENT_*`。

**29. [中] Kconfig 默认 SSID/密码/内网 IP 入库**
- 位置：`main/Kconfig.projbuild`
- 建议：默认留空、强制配网，密钥不进默认值。
- 状态：**已修复**（2026-09-14）：SSID/密码/IP/网关/掩码/WS URL 默认值全部清空，静态 IP 默认关；空配置走 `agent_cfg` 校验失败 → 等待配网。`sdkconfig` 本就不入库，现网设备 NVS 配置不受影响。

**30. [低] `split_ws_host_port` 两处重复**
- 位置：`main/net_ws.c` 与 `main/prov_cfg.c`
- 建议：抽公共 `ws_url_parse()`。
- 状态：**已修复**（2026-09-14）：新 `main/ws_url.c/h`（`ws_url_split_host_port`），`net_ws.c`/`prov_cfg.c` 两处静态副本删除。

**31. [低] 按键任务归属混乱**
- 位置：`main/btn_boot.c`（另建 pri6 任务）+ `main/main.c`（残留空调用 `btn_boot_poll`）
- 建议：统一轮询归属；按钮任务优先级降到 ≤ LVGL。
- 状态：**已修复**（2026-09-14）：删除 `btn_boot_poll()` 空桩及 `main.c` 残留调用；`btn_task` 优先级 6 → 4（= LVGL，不再抢占 UI）。

**32. [低] `ui_msg` 枚举值不连续**
- 位置：`main/ui_msg.h`（`AP_PROV=11`）
- 建议：顺延编号，纯可维护性。
- 状态：**已修复**（2026-09-14）：枚举重排为连续 1..10（顺带移除无发送方的 `UI_MSG_FRAME_DIRTY`）。

### 1.6 功耗

**33. [中] WiFi 永不休眠 + CPU 常 240MHz**
- 位置：`main/net_ws.c:645/684/730`（`WIFI_PS_NONE`）+ `sdkconfig`（`CONFIG_PM_ENABLE` 未开）
- 建议：空闲（无录音/播放/配网）切 `WIFI_PS_MIN_MODEM`；评估降 160MHz；电池场景再评估 light sleep（需权衡 WS 心跳）。
- 状态：**已修复**（2026-09-14）：`net_loop` 每轮 `wifi_ps_apply()`——语音会话/播放中保持 `PS_NONE`，空闲自动切 `MIN_MODEM`（变化才下发）；CPU 保持 240MHz（动画/字幕跟手优先，USB 供电无续航压力，记录为评估结论）。

**34. [中] 静帧时 LVGL 仍 5ms 满速循环**
- 建议：无脏帧时 `vTaskDelay` 拉长到 15–30ms，脏时回 5ms，Core 1/SPI 空闲降耗。
- 状态：**已修复**（2026-09-14）：`ui_loop_once` 返回建议延迟（忙 5ms/闲 30ms），`lvgl_task` 按此休眠；忙碌判定含表情脏、消息处理、语音听/播、配网、诊断、麦克风提示。

**35. [低] PDM 常开听音**
- 说明：设计如此；确保「语音关」/配网路径完整调用 `i2s_channel_disable` 即可。
- 状态：**已核查**（2026-09-14）：`voice_set_enabled(false)` → `audio_capture_listen_stop()` → `i2s_channel_disable(s_rx)` ✓；BLE 配网入口同样停 PDM ✓；AP 配网不占内部 RAM 大块、无需停音频（设计如此）。

---

## 二、后端（backend/）

### 2.1 安全

**36. [高] WebSocket 无鉴权**
- 位置：`backend/ws_manager.py` / `backend/main.py` `/ws`
- 问题：局域网内任意主机可冒充 `device` 或订阅 dashboard，劫持状态/语音通道。
- 建议：token/query 预共享密钥；或 uvicorn 仅绑 `127.0.0.1`。
- 状态：**已修复**（2026-09-14）：`backend/auth.py` 新增 `AGENT_API_TOKEN`；WS 连接校验 `?token=` / `x-api-token`；Dashboard 自动带 Bearer 与 WS token。

**37. [高] 全部 HTTP API 无认证**
- 位置：`/event*`、`/api/llm*`、`/api/ble*`、`/api/wifi*`、`/voice/*`
- 问题：可远程改 LLM Key、读 WiFi 密码、触发 BLE 配网、上传语音。
- 建议：简单 Bearer token；默认绑 localhost，需局域网再显式放开。
- 状态：**已修复**（2026-09-14）：`AuthMiddleware` 保护 `/api/`、`/event`、`/voice/`；`/health` 与静态页保持开放。

**38. [高] WiFi 密码明文落盘且 API 原样返回**
- 位置：`backend/wifi_profiles_store.py` + `/api/status`
- 建议：磁盘加密或 OS 凭据库；API 默认 mask，编辑时再单独取。
- 状态：**已修复**（2026-09-14）：`list_profiles`/`get_profile` 默认 `password=********`；新增 `GET /api/wifi-profiles/{id}?reveal=1` 取明文（需鉴权）。

**39. [高] BLE 日志泄露 WiFi 密码**
- 位置：`backend/ble_prov.py:166`、`:293`（`-> {line}` 把 `WIFI:ssid,password` 打进日志/Dashboard）
- 建议：日志对 password 打码；设备 LIST 回复中的 pass 同样脱敏。
- 状态：**已修复**（2026-09-14）：`sanitize_ble_line` 脱敏 `WIFI:` 与 `pass=`；BLE 收发日志与 `replies` 均打码。

**40. [中] Dashboard ble-log `innerHTML` 未转义**
- 位置：`backend/dashboard.html:566-571`
- 问题：叠加明文密码日志可 XSS。
- 建议：一律 `esc(msg)`；后端先脱敏。
- 状态：**已修复**（2026-09-14）：BLE 日志渲染改 `esc()`；配合后端脱敏。

**41. [中] LLM Key 明文存储**
- 位置：`backend/llm_config.py` / `llm.json`（已 gitignore，mask 仅用于展示）
- 建议：可选 DPAPI/keyring；备份脚本排除。
- 状态：**已评估，保持现状**（2026-09-14）：`llm.json` 已 gitignore、Dashboard mask 展示；DPAPI/keyring 属可选增强，留待有备份需求时再做。

**42. [中] 绑定地址过宽**
- 建议：默认 `127.0.0.1`，需要局域网访问时显式配置 `0.0.0.0` 并配合鉴权。
- 状态：**已修复**（2026-09-14）：`AGENT_BIND_HOST` 默认 `127.0.0.1`（`_run_hidden.py`/`start.bat`）；非 localhost 且无 token 时启动告警。

**43. [低] API 返回本机绝对路径**
- 位置：`backend/llm_history.py:142`（`path` 字段）
- 建议：去掉或仅调试开关返回。
- 状态：**已修复**（2026-09-14）：`query_records` 响应移除 `path` 字段。

### 2.2 并发与异步

**44. [高] 语音流水线跑在 daemon 线程 + 独立事件循环**
- 位置：`backend/voice_pipeline.py:417`（线程内 `asyncio.run`）
- 问题：与 uvicorn 主 loop 分裂，跨 loop 靠 `run_coroutine_threadsafe`；线程未登记、shutdown 无法取消，busy 超时与仍在跑的 pipeline 打架。
- 建议：改主 loop 上 `asyncio.create_task`，阻塞段用 `asyncio.to_thread`；保存 Task 句柄支持取消与优雅关停。
- 状态：**已修复**（2026-09-14）：`run_pipeline` 改主 loop `create_task`；`bind_loop` + `cancel_pipeline`/`cancel_all_pipelines`；阻塞 ASR/TTS 走 `to_thread`。

**45. [高] 流式 ASR 阻塞事件循环**
- 位置：`backend/ws_manager.py:242`、`:299`（async handler 内同步调 Vosk `AcceptWaveform`，CPU 密集）
- 问题：Dashboard 推送、设备 ping、下行音频全被卡住。
- 建议：`await asyncio.to_thread(asr.accept, data)`，或专用线程 + 队列喂音频。
- 状态：**已修复**（2026-09-14）：流式/整包 ASR `accept`/`finish` 均 `asyncio.to_thread`。

**46. [高] hook 队列读取非原子，丢事件**
- 位置：`backend/main.py:411-412`（`read_text` 后立刻 `write_text("")`）
- 问题：与 hook 写入并发时，读清之间的新事件被抹掉。
- 建议：rename 换文件再读，或文件锁/追加 offset。
- 状态：**已修复**（2026-09-14）：抽出 `backend/hook_bridge.py`，`rename` → `.processing` 再读删。

**47. [高] busy 超时不取消僵尸流水线**
- 位置：`backend/ws_manager.py:698-707`
- 问题：超时只清 busy/session 映射，pipeline 线程、ASR/TTS 仍在跑，继续向设备写音频。
- 建议：超时联动 cancel + `session_store.delete` + `finish` ASR/TTS。
- 状态：**已修复**（2026-09-14）：busy 超时调用 `cancel_pipeline`/`cancel_all_pipelines` 并清 stream ASR。

**48. [中] 启动预热同步执行**
- 位置：`backend/main.py:445`（lifespan 内 `voice_warmup()` 触发 SAPI/COM、模型探测）
- 建议：`await asyncio.to_thread(voice_warmup)`。
- 状态：**已修复**（2026-09-14）：lifespan 内 `await asyncio.to_thread(voice_warmup)`。

**49. [中] async 路由内同步读写 JSON**
- 位置：`backend/main.py` / `settings_store.py` / `llm_config.py` / `wifi_profiles_store.py`
- 建议：热路径走内存缓存 + 写盘 `to_thread`；或统一 async file lock。
- 状态：**已评估，保持现状**（2026-09-14）：各 store 已有内存缓存 + 锁；写盘频率低，全量 `to_thread` 收益有限，留待热路径 profiling 后再做。

**50. [低] SAPI TTS 全进程串行**
- 位置：`backend/tts_sapi.py`（全局 `_LOCK` + COM）
- 说明：单机可接受；多段并发播报需求出现时考虑进程池或 edge-tts 分流。
- 状态：**已核查**（2026-09-14）：单机单设备场景可接受，设计如此。

### 2.3 性能

**51. [高] 流式录音 O(n²) 拼接**
- 位置：`backend/voice_session.py:68`（`self.pcm_data += data`）
- 建议：改 `bytearray` 或 `list[bytes]` 结束后 join，长录音 CPU/内存明显下降。
- 状态：**已修复**（2026-09-14）：`pcm_data` 改 `bytearray.extend`。

**52. [高] TTS 全量 PCM 与 chunk 双份驻留**
- 位置：`backend/voice_session.py:91`（`self.audio_pcm += pcm` 同时切 chunk 队列入 deque）
- 建议：只保留 deque chunks，总长度用计数器，长回复内存减半。
- 状态：**已修复**（2026-09-14）：移除 `audio_pcm` 双份驻留，仅 `audio_chunks` + `audio_total_bytes` 计数。

**53. [高] LLM 历史无轮转、查询全量读**
- 位置：`backend/llm_history.py`（`query_records` 每次全量读 jsonl 再过滤）
- 建议：按日切分/上限 N 行；查询反向读或迁 SQLite。
- 状态：**已修复**（2026-09-14）：追加时 `LLM_HISTORY_MAX_LINES` 轮转；查询默认 tail `LLM_HISTORY_QUERY_TAIL` 行。

**54. [中] Dashboard 每秒全量轮询**
- 位置：`backend/dashboard.html:1130`（`setInterval(refresh, 1000)` 拉全量 `/api/status`，含 profiles/logs/transcripts/tts voices）
- 建议：WS 推送增量；HTTP 降 3–5s 或分字段拉取。
- 状态：**部分完成**（2026-09-14）：HTTP 轮询 1s → 3s；增量仍靠 WS `backend_log`/`display`，全量 WS 推送留待后续。

**55. [低] Vosk 缓冲额外拷贝**
- 位置：`backend/asr.py`（`bytes(self._buf[:N]); del`）
- 建议：`memoryview` 或环形缓冲。
- 状态：**已修复**（2026-09-14）：切片改 `memoryview` 再 `bytes()`。

**56. [低] `scale_pcm16` 死代码**
- 位置：`backend/settings_store.py`
- 说明：音量已下推设备端，链路几乎不用；删除或在 TTS enqueue 前统一调用。
- 状态：**已修复**（2026-09-14）：删除未使用的 `scale_pcm16`。

### 2.4 健壮性

**57. [中] `except Exception` 过宽**
- 位置：`backend/ws_manager.py` / `ble_prov.py` / `voice_pipeline.py` / `main.py` BLE 路由
- 建议：收窄异常类型；关键路径结构化日志 + 错误码，排障更快。
- 状态：**部分完成**（2026-09-14）：`voice_pipeline`/`ws_manager` ASR 路径收窄为 `RuntimeError`/`OSError`/`ValueError`；BLE 路由仍保留宽捕获兜底。

**58. [中] 会话无定时清扫**
- 位置：`backend/voice_session.py`（仅 `create`/`get` 时 `_cleanup`）
- 建议：pump 周期扫过期 session，长时间运行内存更稳。
- 状态：**已修复**（2026-09-14）：`session_store.cleanup_expired()` 在 `run_session_pump` 每轮调用。

**59. [中] LLM/TTS 缺分级超时**
- 位置：`backend/llm.py`（httpx timeout=120 单一超时）
- 建议：LLM 首字节/流式/TTS 分级超时；失败时清空 `tts_q`，快速回 IDLE。
- 状态：**部分完成**（2026-09-14）：`httpx.Timeout` 分拆 connect/read/write；TTS 队列取消仍随 pipeline `cancel` 联动。

**60. [低] 临时 wav 可能残留**
- 位置：`backend/asr.py`（NamedTemporaryFile 依赖 finally）
- 建议：`TemporaryDirectory` 或启动时清 `sapi_tts_*`/`*.wav`。
- 状态：**已评估，保持现状**（2026-09-14）：`transcribe_pcm` 已有 `finally: os.unlink`；非 vosk 路径使用频率低。

### 2.5 代码质量

**61. [中] `main.py` 880 行单体**
- 建议：拆 `routes_*.py` / `hook_bridge.py`，路由、hook、状态机分离。
- 状态：**部分完成**（2026-09-14）：抽出 `hook_bridge.py`、`auth.py`、`sanitize.py`；路由仍集中在 `main.py`。

**62. [中] `run_pipeline` 超长嵌套闭包**
- 位置：`backend/voice_pipeline.py`
- 建议：拆 `run_asr` / `run_llm_stream` / `run_tts_stream`，易测易改。
- 状态：**部分完成**（2026-09-14）：改 async task 架构并收窄异常；函数体拆分留待后续。

**63. [中] BLE provision 与 read_profiles 重复近百行**
- 位置：`backend/ble_prov.py`
- 建议：抽公共 `NusSession` 上下文管理器，连接失败重试策略显式化。
- 状态：**部分完成**（2026-09-14）：统一 `sanitize_ble_line` 日志脱敏；`NusSession` 抽取留待后续。

**64. [中] ASR 模型路径硬编码本机**
- 位置：`backend/asr.py:24-26`（`D:/0-C/.../vosk-model-*`）
- 建议：仅相对 `backend/models` + 环境变量。
- 状态：**已修复**（2026-09-14）：移除 `D:/0-C` 硬编码，仅 `backend/models` + `VOSK_MODEL_PATH`。

**65. [低] 全局单例遍地**
- 位置：`ws_hub`、`session_store`、`chat_context`、`ble_prov`
- 建议：经 FastAPI `app.state` 注入，可单测。
- 状态：**已评估，保持现状**（2026-09-14）：单进程桌面后端，注入重构收益低。

**66. [低] `chat_context` 单设备假设**
- 说明：注释已写明；多设备扩展时按 device_id 分桶。
- 状态：**已核查**（2026-09-14）：注释已说明，当前单设备设计。

**67. [低] 类型注解宽松**
- 位置：`backend/ws_manager.py`（`stream_asr: Any` 等）
- 建议：Protocol 约束 push/transcript 回调签名。
- 状态：**已评估，保持现状**（2026-09-14）：纯可维护性增强，非功能缺陷。

### 2.6 依赖与部署

**68. [中] `requirements.txt` 几乎未钉版本**
- 位置：`backend/requirements.txt`（仅 `bleak>=0.22.0`）
- 建议：`pip freeze` 生成锁文件；可选依赖分组（`[windows]`、`[asr-vosk]`、`[asr-whisper]`）。
- 状态：**已修复**（2026-09-14）：主要依赖加兼容上界；`pywin32` 限 `sys_platform == win32`；新增 `scripts/requirements.txt`（Pillow）。

**69. [中] 强绑 Windows SAPI**
- 位置：`backend/tts_sapi.py` + `pywin32`
- 建议：文档标明 Windows 一等公民；非 Win 自动降级 edge-tts，import 失败友好提示。
- 状态：**已核查**（2026-09-14）：`voice_pipeline` 已按 `is_local_voice` 分流 edge-tts；`pywin32` 为可选平台依赖。

**70. [低] `.env.example` 不完整**
- 缺：`ASR_ENGINE`、`VOSK_MODEL_PATH`、`TTS_VOICE`、`TTS_RATE`、`CHAT_CONTEXT_*`、`WHISPER_*`、绑定地址/鉴权等。
- 建议：按 README 环境变量表补全并加安全提示。
- 状态：**已修复**（2026-09-14）：`backend/.env.example` 补全绑定/鉴权/ASR/TTS/历史变量与安全提示。

**71. [低] HTML 静态页每次读盘**
- 位置：`backend/main.py:466-483`
- 建议：启动时读入内存或 `FileResponse`。
- 状态：**已修复**（2026-09-14）：`dashboard.html`/`chat_history.html` 模块加载时读入内存缓存。

---

## 三、工程化

### 3.1 版本控制

**72. [高] `backend/settings.json` 误入库且已被本地改脏**
- 证据：`git status` 显示 `M backend/settings.json`（音量/开关/音色等运行时偏好）；同类 `wifi_profiles.json`、`llm.json` 已正确 ignore。
- 建议：加入 `.gitignore`，仓库改存 `settings.json.example`；`settings_store` 在文件缺失时写默认值（默认常量已存在）。
- 状态：**已修复**（2026-09-14）：`.gitignore` 加入 `backend/settings.json`；新增 `backend/settings.json.example`。

**73. [高] `.vscode/settings.json` 含本机绝对路径**
- 证据：跟踪文件中有 `D:\Espressif\...`、`d:\0-C\esp32s3-agent-display\build`。
- 建议：改 `.vscode/settings.json.example`，或拆用户级配置，只提交通用项。
- 状态：**已修复**（2026-09-14）：新增 `.vscode/settings.json.example`；跟踪的 `settings.json` 加入 `.gitignore`。

**74. [中] 未跟踪垃圾文件 `nul`**
- 证据：`git status` → `?? nul`（Windows 重定向产物）。
- 建议：`.gitignore` 增加 `/nul` 并删除该文件。
- 状态：**已修复**（2026-09-14）：`.gitignore` 增加 `/nul`（Windows 保留名，无法普通删除）。

**75. [中] 约 7MB 二进制入库且无 Git LFS**
- 证据：`animations.bin` 4.4MB + `font` 0.9MB + `release.bin` 1.5MB，无 `.gitattributes`，仓库跟踪总量约 24MB。
- 建议：Git LFS 或 Release 附件托管，仓库只留生成脚本与校验和（release README 已有 SHA256 可复用）。
- 状态：**已评估，保持现状**（2026-09-14）：固件资源需随仓库分发；LFS/Release 拆分属发布流程改造，留待发版时迁移。

**76. [低] `dependencies.lock` 被 ignore**
- 说明：要可复现构建可提交 lock；与 `idf_component.yml` caret 范围二选一。
- 状态：**已核查**（2026-09-14）：与 `idf_component.yml` caret 策略一致，保持 ignore。

### 3.2 构建与脚本

**77. [高] `scripts/idf_build.bat` 完全硬编码本机路径**
- 证据：`IDF_PATH=D:\Espressif\.espressif\v5.4.2\esp-idf` 及 tools/python/cmake 全写死。
- 建议：优先读环境变量、缺省回退；或生成 gitignore 的 `idf_build.local.bat` + 提交模板。
- 状态：**已修复**（2026-09-14）：`idf_build.bat` 仅依赖 `IDF_PATH`/`IDF_PYTHON_ENV_PATH`；附 `idf_build.bat.example`。

**78. [中] flash 脚本默认串口不一致**
- 位置：`scripts/flash_animations.py`（COM4）vs `scripts/flash_font.py`（COM5）
- 建议：统一必填 `-p` 参数或统一默认值。
- 状态：**已修复**（2026-09-14）：统一 `-p` 或 `ESPPORT` 必填（`scripts/flash_util.require_port`）。

**79. [中] esptool/python 回退路径绑死 `D:\Espressif`**
- 位置：`scripts/flash_*.py` 的 `find_python` / `find_esptool`
- 建议：只依赖 `IDF_PATH` / `IDF_PYTHON_ENV_PATH` + PATH。
- 状态：**已修复**（2026-09-14）：抽出 `scripts/flash_util.py`，移除 `D:\Espressif` 硬编码。

**80. [中] `gen_frame_player.py` 缺分区上限校验与参数化**
- 对比：`gen_cjk_font.py` 有分区上限检查。
- 建议：加 argparse、缺文件明确退出码、`len(blob)` 超 0x600000 报错。
- 状态：**已修复**（2026-09-14）：argparse + 缺 GIF 退出码 2；超 `0x470000`（animations 分区）退出码 3。

**81. [中] 资源脚本依赖未声明**
- 证据：`gen_frame_player.py` 用 Pillow，README 提及但 `backend/requirements.txt` 无。
- 建议：新增 `scripts/requirements.txt`。
- 状态：**已修复**（2026-09-14）：新增 `scripts/requirements.txt`（Pillow）。

**82. [低] CMake 依赖未显式声明**
- 位置：`main/CMakeLists.txt` 未列 `lvgl` / `esp_websocket_client`（代码在用）。
- 建议：`REQUIRES`/`PRIV_REQUIRES` 显式声明。
- 状态：**已修复**（2026-09-14）：`REQUIRES` 增加 `lvgl__lvgl`、`espressif__esp_websocket_client`。

**83. [低] 遗留空 `spiffs` 分区**
- 位置：`partitions.csv`（896KB 遗留空 + `storage` 4MB）。
- 建议：清理或合并；不做双槽 OTA 则文档标明 `otadata` 仅预留。
- 状态：**已修复**（2026-09-14）：项 24 OTA 重排时已删除遗留空 `spiffs`，`storage` 后移至 `0xC80000`。

### 3.3 配置管理

**84. [中] README 与代码默认值不一致**
- 证据：README 写 `VOICE_TTS` 默认 `0`；`backend/voice_pipeline.py` 为 `os.getenv(..., "1")`，`.env.example` 也是 `1`。
- 建议：统一文档与代码默认值。
- 状态：**已修复**（2026-09-14）：`voice_pipeline.py` 与 `.env.example` 默认均改为 `0`（与 README 一致）。

**85. [低] docs 偏「本机安装日记」**
- 建议：文首加「适用 IDF 5.4.2 / 最后核对日期」，与构建脚本模板联动。
- 状态：**已评估，保持现状**（2026-09-14）：`idf_build.bat.example` 已提供模板；README 大改留待专门文档整理。

### 3.4 测试与 CI

**86. [高] 项目自有测试为零**
- 证据：无 `pytest`/`unity`/任何测试目录（仅 `managed_components` 自带）。
- 建议：后端先测 `settings_store` / LLM URL 拼接 / WS 协议；固件关键解析（anim header）做主机侧单测。
- 状态：**部分完成**（2026-09-14）：新增 `tests/`（auth/sanitize/settings/hook/llm_history，9 项）；固件 anim 主机侧单测留待后续。

**87. [高] 无 CI**
- 证据：无 `.github/`。
- 建议：最小流水线：`pip install` + ruff + pytest；可选 IDF Docker build。
- 状态：**已修复**（2026-09-14）：`.github/workflows/ci.yml`（ruff + pytest）；IDF Docker build 留作可选增强。

---

## 四、建议实施顺序

| 批次 | 内容 | 涉及条目 | 状态 |
|------|------|----------|------|
| 第一批：立即 | 安全脱敏（WiFi 密码日志/API 打码、WS/HTTP 鉴权或绑 localhost）；`settings.json` 出库改 example；固件栈/DMA 修复 | #36-39, #72, #3, #10 | **已完成**（固件 2026-09-14；后端/工程化 2026-09-14） |
| 第二批：本周 | 表情 blit/并发；ASR 移出事件循环 + pipeline 可取消；PCM 优化；hook 原子；依赖/env；`idf_build.bat` | #15-16, #8, #44-45, #51-52, #46, #68, #70, #77 | **已完成** |
| 第三批：随后 | 历史轮转；pytest + CI；WiFi/STALE/WDT；PSRAM/OTA；功耗；代码拆分 | #53, #86-87, #25, #21, #22, #2, #1, #24, #33-34, #27, #61 | **已完成**（#86/#61/#63 等为部分完成，见各条状态） |

> 注：以上优化均遵守项目设计红线（音频绑 Core 0、LVGL API 仅 lvgl 任务调用、离线 RLE + Core 1 SPI 直绘、PDM DATA=GPIO16）。

### 局域网部署提示（新增）

1. 在 `backend/.env` 设置 `AGENT_BIND_HOST=0.0.0.0` 与 `AGENT_API_TOKEN=<强随机串>`。
2. 浏览器打开 Dashboard 时带一次 `?token=<同上>`，或 `localStorage.setItem('agent_api_token', '...')`。
3. ESP/WebSocket 客户端需在 URL 附加相同 `token` 查询参数。
