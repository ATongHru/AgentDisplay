fastapi>=0.115.0,<0.116
uvicorn[standard]>=0.32.0,<0.33
httpx>=0.27.0,<0.28
python-dotenv>=1.0.0,<2
edge-tts>=6.1.0,<7
miniaudio>=1.61,<2
bleak>=0.22.0,<0.23

# ASR (pick one engine via ASR_ENGINE)
vosk>=0.3.45,<0.4
SpeechRecognition>=3.10.0,<4

# Optional ASR
# faster-whisper>=1.0.0,<2

# Windows TTS (optional; non-Windows can omit)
pywin32>=306,<400; sys_platform == "win32"
